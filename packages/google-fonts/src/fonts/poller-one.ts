// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Poller One";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/pollerone/v25/ahccv82n0TN3gia5E4BuR-5RgA.woff2",
  },
} as const;

export type PollerOneWeight = "400";
export type PollerOneStyle = "normal";
export type PollerOneSubset = "latin";
export type PollerOneOptions = GoogleFontOptions<PollerOneWeight, PollerOneStyle, PollerOneSubset>;

/**
 * Poller One — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class PollerOne extends GoogleFont<
  PollerOneWeight,
  PollerOneStyle,
  PollerOneSubset
> {
  constructor(options?: PollerOneOptions) {
    super(FAMILY, FACES, options);
  }
}
