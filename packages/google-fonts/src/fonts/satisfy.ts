// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Satisfy";

const FACES = {
  "latin": {
    "400-normal": "https://fonts.gstatic.com/s/satisfy/v22/rP2Hp2yn6lkG50LoCZOIHQ.woff2",
  },
} as const;

export type SatisfyWeight = "400";
export type SatisfyStyle = "normal";
export type SatisfySubset = "latin";
export type SatisfyOptions = GoogleFontOptions<SatisfyWeight, SatisfyStyle, SatisfySubset>;

/**
 * Satisfy — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Satisfy extends GoogleFont<SatisfyWeight, SatisfyStyle, SatisfySubset> {
  constructor(options?: SatisfyOptions) {
    super(FAMILY, FACES, options);
  }
}
