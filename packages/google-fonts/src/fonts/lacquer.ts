// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Lacquer";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/lacquer/v16/EYqzma1QwqpG4_BBN7iKXw.woff2",
  },
} as const;

export type LacquerWeight = "400";
export type LacquerStyle = "normal";
export type LacquerSubset = "latin";
export type LacquerOptions = GoogleFontOptions<LacquerWeight, LacquerStyle, LacquerSubset>;

/**
 * Lacquer — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Lacquer extends GoogleFont<LacquerWeight, LacquerStyle, LacquerSubset> {
  constructor(options?: LacquerOptions) {
    super(FAMILY, FACES, options);
  }
}
