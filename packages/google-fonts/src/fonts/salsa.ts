// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Salsa";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/salsa/v23/gNMKW3FiRpKj-hmf-HY.woff2",
  },
} as const;

export type SalsaWeight = "400";
export type SalsaStyle = "normal";
export type SalsaSubset = "latin";
export type SalsaOptions = GoogleFontOptions<SalsaWeight, SalsaStyle, SalsaSubset>;

/**
 * Salsa — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Salsa extends GoogleFont<SalsaWeight, SalsaStyle, SalsaSubset> {
  constructor(options?: SalsaOptions) {
    super(FAMILY, FACES, options);
  }
}
