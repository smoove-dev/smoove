// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Artifika";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/artifika/v22/VEMyRoxzronptCuxu5Wq7DE.woff2",
  },
} as const;

export type ArtifikaWeight = "400";
export type ArtifikaStyle = "normal";
export type ArtifikaSubset = "latin";
export type ArtifikaOptions = GoogleFontOptions<ArtifikaWeight, ArtifikaStyle, ArtifikaSubset>;

/**
 * Artifika — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Artifika extends GoogleFont<ArtifikaWeight, ArtifikaStyle, ArtifikaSubset> {
  constructor(options?: ArtifikaOptions) {
    super(FAMILY, FACES, options);
  }
}
