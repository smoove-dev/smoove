// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Frijole";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/frijole/v15/uU9PCBUR8oakM2BQ3xTR3w.woff2",
  },
} as const;

export type FrijoleWeight = "400";
export type FrijoleStyle = "normal";
export type FrijoleSubset = "latin";
export type FrijoleOptions = GoogleFontOptions<FrijoleWeight, FrijoleStyle, FrijoleSubset>;

/**
 * Frijole — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Frijole extends GoogleFont<FrijoleWeight, FrijoleStyle, FrijoleSubset> {
  constructor(options?: FrijoleOptions) {
    super(FAMILY, FACES, options);
  }
}
