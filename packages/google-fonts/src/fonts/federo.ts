// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Federo";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/federo/v20/iJWFBX-cbD_ETsbWilmf.woff2",
  },
} as const;

export type FederoWeight = "400";
export type FederoStyle = "normal";
export type FederoSubset = "latin";
export type FederoOptions = GoogleFontOptions<FederoWeight, FederoStyle, FederoSubset>;

/**
 * Federo — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Federo extends GoogleFont<FederoWeight, FederoStyle, FederoSubset> {
  constructor(options?: FederoOptions) {
    super(FAMILY, FACES, options);
  }
}
