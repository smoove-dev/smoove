// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Acme";

const FACES = {
  "latin": {
    "400-normal": "https://fonts.gstatic.com/s/acme/v27/RrQfboBx-C5_XxrBbg.woff2",
  },
} as const;

export type AcmeWeight = "400";
export type AcmeStyle = "normal";
export type AcmeSubset = "latin";
export type AcmeOptions = GoogleFontOptions<AcmeWeight, AcmeStyle, AcmeSubset>;

/**
 * Acme — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Acme extends GoogleFont<AcmeWeight, AcmeStyle, AcmeSubset> {
  constructor(options?: AcmeOptions) {
    super(FAMILY, FACES, options);
  }
}
