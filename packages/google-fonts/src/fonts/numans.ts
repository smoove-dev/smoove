// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Numans";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/numans/v16/SlGRmQmGupYAfH84ZhIh.woff2",
  },
} as const;

export type NumansWeight = "400";
export type NumansStyle = "normal";
export type NumansSubset = "latin";
export type NumansOptions = GoogleFontOptions<NumansWeight, NumansStyle, NumansSubset>;

/**
 * Numans — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Numans extends GoogleFont<NumansWeight, NumansStyle, NumansSubset> {
  constructor(options?: NumansOptions) {
    super(FAMILY, FACES, options);
  }
}
