// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Handlee";

const FACES = {
  "latin": {
    "400-normal": "https://fonts.gstatic.com/s/handlee/v20/-F6xfjBsISg9aMakPm3wow.woff2",
  },
} as const;

export type HandleeWeight = "400";
export type HandleeStyle = "normal";
export type HandleeSubset = "latin";
export type HandleeOptions = GoogleFontOptions<HandleeWeight, HandleeStyle, HandleeSubset>;

/**
 * Handlee — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Handlee extends GoogleFont<HandleeWeight, HandleeStyle, HandleeSubset> {
  constructor(options?: HandleeOptions) {
    super(FAMILY, FACES, options);
  }
}
