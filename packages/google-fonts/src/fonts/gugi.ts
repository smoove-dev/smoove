// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Gugi";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/gugi/v21/A2BVn5dXywshZAmK9Q.woff2",
  },
} as const;

export type GugiWeight = "400";
export type GugiStyle = "normal";
export type GugiSubset = "latin";
export type GugiOptions = GoogleFontOptions<GugiWeight, GugiStyle, GugiSubset>;

/**
 * Gugi — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Gugi extends GoogleFont<GugiWeight, GugiStyle, GugiSubset> {
  constructor(options?: GugiOptions) {
    super(FAMILY, FACES, options);
  }
}
