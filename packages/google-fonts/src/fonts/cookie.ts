// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Cookie";

const FACES = {
  "latin": {
    "400-normal": "https://fonts.gstatic.com/s/cookie/v23/syky-y18lb0tSbf9kgqS.woff2",
  },
} as const;

export type CookieWeight = "400";
export type CookieStyle = "normal";
export type CookieSubset = "latin";
export type CookieOptions = GoogleFontOptions<CookieWeight, CookieStyle, CookieSubset>;

/**
 * Cookie — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Cookie extends GoogleFont<CookieWeight, CookieStyle, CookieSubset> {
  constructor(options?: CookieOptions) {
    super(FAMILY, FACES, options);
  }
}
