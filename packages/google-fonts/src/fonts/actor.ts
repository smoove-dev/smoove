// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Actor";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/actor/v18/wEOzEBbCkc5cO0ejVSk.woff2",
  },
} as const;

export type ActorWeight = "400";
export type ActorStyle = "normal";
export type ActorSubset = "latin";
export type ActorOptions = GoogleFontOptions<ActorWeight, ActorStyle, ActorSubset>;

/**
 * Actor — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Actor extends GoogleFont<ActorWeight, ActorStyle, ActorSubset> {
  constructor(options?: ActorOptions) {
    super(FAMILY, FACES, options);
  }
}
