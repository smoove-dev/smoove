// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Allkin";

const FACES = {
  fallback: {
    "400-normal": "https://fonts.gstatic.com/s/allkin/v10/ieV_2Y5KIGmGKpnzKA.woff2",
  },
} as const;

export type AllkinWeight = "400";
export type AllkinStyle = "normal";
export type AllkinSubset = "fallback";
export type AllkinOptions = GoogleFontOptions<AllkinWeight, AllkinStyle, AllkinSubset>;

/**
 * Allkin — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Allkin extends GoogleFont<AllkinWeight, AllkinStyle, AllkinSubset> {
  constructor(options?: AllkinOptions) {
    super(FAMILY, FACES, options);
  }
}
