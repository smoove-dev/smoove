// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Wallpoet";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/wallpoet/v21/f0X10em2_8RnXVVdUObp58Q.woff2",
  },
} as const;

export type WallpoetWeight = "400";
export type WallpoetStyle = "normal";
export type WallpoetSubset = "latin";
export type WallpoetOptions = GoogleFontOptions<WallpoetWeight, WallpoetStyle, WallpoetSubset>;

/**
 * Wallpoet — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Wallpoet extends GoogleFont<WallpoetWeight, WallpoetStyle, WallpoetSubset> {
  constructor(options?: WallpoetOptions) {
    super(FAMILY, FACES, options);
  }
}
