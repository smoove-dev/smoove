// AUTO-GENERATED by scripts/generate.ts — do not edit.
import { GoogleFont, type GoogleFontOptions } from "../runtime.js";

const FAMILY = "Rancho";

const FACES = {
  latin: {
    "400-normal": "https://fonts.gstatic.com/s/rancho/v22/46kulbzmXjLaqZRVam_h.woff2",
  },
} as const;

export type RanchoWeight = "400";
export type RanchoStyle = "normal";
export type RanchoSubset = "latin";
export type RanchoOptions = GoogleFontOptions<RanchoWeight, RanchoStyle, RanchoSubset>;

/**
 * Rancho — Google font. Pass `weights`/`styles` to register a subset of
 * faces (omit for all) and `subset` to choose the character set (default "latin").
 */
export default class Rancho extends GoogleFont<RanchoWeight, RanchoStyle, RanchoSubset> {
  constructor(options?: RanchoOptions) {
    super(FAMILY, FACES, options);
  }
}
